// Utilidades para el RUT chileno.

// Normaliza un RUT: quita puntos y guión, deja el dígito verificador en mayúscula.
export function normalizeRut(rut: string): string {
  // TODO: implementar limpieza de formato.
  return rut;
}

// Valida el dígito verificador de un RUT (módulo 11).
export function isValidRut(rut: string): boolean {
  // TODO: implementar validación módulo 11.
  return false;
}
