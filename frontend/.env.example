# En dev normalmente no hace falta: el proxy de Vite reenvía /api al backend.
# Para apuntar a otro backend en dev:
# VITE_API_TARGET=http://localhost:3000
#
# Para un build servido aparte del backend, fijar la URL absoluta de la API:
# VITE_API_URL=https://mi-backend.example.com
